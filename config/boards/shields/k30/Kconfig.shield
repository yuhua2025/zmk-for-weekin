# Copyright (c) 2020 The ZMK Contributors
# SPDX-License-Identifier: MIT

config SHIELD_K30
	def_bool $(shields_list_contains,k30)
	select ZMK_LED_BREATHING if ZMK_RGB_UNDERGLOW
	select GPIO  # 始终选择GPIO以支持LED控制

# 确保GPIO驱动被启用以支持LED控制
config GPIO
	default y if SHIELD_K30
